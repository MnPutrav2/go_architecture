export default function App() {
    return (
        <main className="container">
            <h1>Golang</h1>

            <p>
                Golang structure project like laravel
            </p>

            <button
                onClick={() => {
                    alert("Hello!");
                }}
            >
                Click Me
            </button>
        </main>
    );
}